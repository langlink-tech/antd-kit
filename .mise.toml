# Greenfield toolchain contract. Keep aligned with packageManager and CI.
# Floors: chezmoi-src/dot_agents/STACK_BASELINE.toml (deployed as ~/.agents/STACK_BASELINE.toml).

[tools]
node = "22"
"npm:pnpm" = "11.10.0"
